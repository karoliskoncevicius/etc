# general-doesn't-fit stuff
color normal     default  default # normal text
color message    color4   default # messages
color error      color1   default # errors
color markers    color7   default # + signs for wrapped text
color tilde      color7   default # filling empty lines with tildes
color status     default  color07 # status lines on top and bottom
color search     default  color11 # search

# sidebar stuff
color sidebar_ordinary  color0   default # mailboxes
color sidebar_new       color0   default # mailboxes with new mail
color sidebar_divider   color15  default # dividing characters
# color sidebar_indicator color0   color15 # selected mailbox

# index stuff
color tree            color7   default         # tree arrows
color indicator       color0   color15         # current selection
color index           default  default  .*     # all messages
color index_date      color7   default         # date column
color index_number    color7   default         # color of numbers
color index_collapsed color7   default         # color of collapsed
color index_label     color3   default         # label column
color index_author    default  default  !~D!~U # author column
color index           color1   default  ~D     # deleted messages
color index           color2   default  ~N!~D  # new messages
color index           color2   default  ~U!~D  # unread messages
color index           color2   default  ~O!~D  # old messages
color index_flags     color7   default  .*     # flags
color index_subject   color4   default  ~E!~D  # expired messages
color index_author    color8   default  ~P!~D  # messages from me
color index_subject   color3   default  ~F!~D  # flagged messages
color index_author    color3   default  ~F!~D  # flagged messages
color index_subject   color4   default  ~T!~D  # tagged messages
color index_author    color4   default  ~T!~D  # tagged messages


# header stuff
color hdrdefault default color15
color header     default color15 "^Subject"
color header     default color15 "^From"
color header     default color15 "^To"

# footer stuff
color signature color7  default # signature
color body      color7  default "^CONFIDENTIALITY NOTICE: .*"
color body      color7  default "^AVIS DE CONFIDENTIALITÉ: .*"
color body      color7  default "^AVIS DE CONFIDENTIALITÉ : .*"

# NOTE: reconsider later if mutt ever allows to add bold without color
# url, email and web stuff
# color body color6 default "(finger|ftp|http|https|news|telnet)://[^ >]*"
# color body color6 default "<URL:[^ ]*>"
# color body color6 default "www\\.[-.a-z0-9]+\\.[a-z][a-z][a-z]?([-_./~a-z0-9]+)?"
# color body color4 default "mailto: *[^ ]+\(\\i?subject=[^ ]+\)?"
# color body color4 default "[-a-z_0-9.%$]+@[-a-z_0-9.]+\\.[-a-z][-a-z]+"

# misc body stuff
color attachment color3  default  # add-ons to the message
# color signature  color3 default

# quote levels
color quoted  color0 color15
color quoted1 color8 color15
color quoted2 color0 color15
color quoted3 color8 color15
color quoted4 color0 color15
color quoted5 color8 color15
color quoted6 color0 color15
color quoted7 color8 color15
color quoted8 color0 color15

# # gpg stuff
# color body       color188 color237 "^gpg: Good signature.*"
# color body       color115 color236 "^gpg: BAD signature.*"
# color body       color174 color237 "^gpg: Can't check signature.*"
# color body       color174 color237 "^-----BEGIN PGP SIGNED MESSAGE-----"
# color body       color174 color237 "^-----BEGIN PGP SIGNATURE-----"
# color body       color174 color237 "^-----END PGP SIGNED MESSAGE-----"
# color body       color174 color237 "^-----END PGP SIGNATURE-----"
# color body       color174 color237 "^Version: GnuPG.*"
# color body       color174 color237 "^Comment: .*"

